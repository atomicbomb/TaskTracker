<Window x:Class="TaskTracker.Views.JiraProjectsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:TaskTracker.Views"
        mc:Ignorable="d"
        Title="TaskTracker - JIRA Projects" 
        Height="600" 
        Width="800"
        MinHeight="400" 
        MinWidth="600"
        Style="{StaticResource MainWindowStyle}"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <!-- Converters -->
        <local:InverseBoolConverter x:Key="InverseBoolConverter" />
        <BooleanToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </Window.Resources>
    
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <TextBlock Grid.Row="0" 
                   Text="JIRA Projects" 
                   FontSize="24" 
                   FontWeight="Bold" 
                   Margin="0,0,0,10"
                   Foreground="{StaticResource TextPrimaryBrush}"/>
        
        <TextBlock Grid.Row="1"
                   Text="Select which JIRA projects you want to track time against. Only projects with active tasks assigned to you will be shown."
                   TextWrapping="Wrap"
                   Foreground="{StaticResource TextSecondaryBrush}"
                   Margin="0,0,0,20"/>
        
        <!-- Toolbar -->
        <Grid Grid.Row="2">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="10"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Refresh from JIRA" 
                        Style="{StaticResource PrimaryButtonStyle}"
                        Command="{Binding RefreshFromJiraCommand}"
                        Margin="0,0,10,0"/>
                <Button Content="Select All" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding SelectAllCommand}"
                        Margin="0,0,10,0"/>
                <Button Content="Select None" 
                        Style="{StaticResource SecondaryButtonStyle}"
                        Command="{Binding SelectNoneCommand}"/>
            </StackPanel>
            
            <!-- Projects DataGrid -->
            <DataGrid Grid.Row="2"
                      ItemsSource="{Binding Projects}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      CanUserReorderColumns="True"
                      CanUserResizeColumns="True"
                      CanUserSortColumns="True"
                      GridLinesVisibility="Horizontal"
                      HeadersVisibility="Column"
                      SelectionMode="Extended"
                      Background="{StaticResource BackgroundBrush}"
                      Foreground="{StaticResource TextPrimaryBrush}"
                      IsEnabled="{Binding IsLoading, Converter={StaticResource InverseBoolConverter}}">
                
                <DataGrid.Columns>
                    <DataGridTemplateColumn Header="Selected" Width="80" CanUserSort="True" SortMemberPath="IsSelected">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <CheckBox IsChecked="{Binding IsSelected, UpdateSourceTrigger=PropertyChanged}" 
                                          HorizontalAlignment="Center"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                    
                    <DataGridTextColumn Header="Project Code" 
                                        Binding="{Binding ProjectCode}" 
                                        Width="150" 
                                        IsReadOnly="True"
                                        CanUserSort="True"/>
                    
                    <DataGridTextColumn Header="Project Name" 
                                        Binding="{Binding ProjectName}" 
                                        Width="*" 
                                        IsReadOnly="True"
                                        CanUserSort="True"/>
                </DataGrid.Columns>
                
                <DataGrid.Style>
                    <Style TargetType="DataGrid">
                        <Setter Property="Background" Value="{StaticResource BackgroundBrush}"/>
                        <Setter Property="Foreground" Value="{StaticResource TextPrimaryBrush}"/>
                        <Setter Property="BorderBrush" Value="{StaticResource BorderBrush}"/>
                        <Setter Property="GridLinesVisibility" Value="Horizontal"/>
                        <Setter Property="HorizontalGridLinesBrush" Value="{StaticResource BorderBrush}"/>
                    </Style>
                </DataGrid.Style>
            </DataGrid>
        </Grid>
        
        <!-- Loading Overlay -->
        <Border Grid.Row="2" 
                Background="{StaticResource SurfaceBrush}" 
                Opacity="0.8"
                Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <ProgressBar IsIndeterminate="True" Width="200" Height="20" Margin="0,0,0,10"/>
                <TextBlock Text="Loading..." 
                           HorizontalAlignment="Center"
                           Foreground="{StaticResource TextPrimaryBrush}"/>
            </StackPanel>
        </Border>
        
        <!-- Status Bar -->
        <Border Grid.Row="3" 
                Background="{StaticResource SurfaceBrush}" 
                BorderBrush="{StaticResource BorderBrush}" 
                BorderThickness="0,1,0,0" 
                Padding="10,5"
                Margin="0,10,0,0">
            <TextBlock Text="{Binding StatusMessage}" 
                       Foreground="{StaticResource TextSecondaryBrush}"/>
        </Border>
        
        <!-- Buttons -->
        <StackPanel Grid.Row="4" 
                    Orientation="Horizontal" 
                    HorizontalAlignment="Right"
                    Margin="0,10,0,0">
            <Button Content="Save Selection" 
                    Style="{StaticResource PrimaryButtonStyle}"
                    Command="{Binding SaveSelectionCommand}"
                    IsDefault="True"
                    Width="120"
                    Margin="0,0,10,0"/>
            <Button Content="Close" 
                    Style="{StaticResource SecondaryButtonStyle}"
                    Command="{Binding CloseCommand}"
                    Width="80"/>
        </StackPanel>
    </Grid>
</Window>
